@model IEnumerable<HtmlBlock>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>HtmlBlocks</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "CreateEdit", new { type = "HtmlBlocks" }, new { @class = "btn" })
    </p>
    <table class="table">
        <thead>
            <tr class="title">
                <th>Название</th>
                <th class="text-center">Действия</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <td> @Html.ActionLink(item.ParentTitle ?? "ERROR", "CreateEdit", new { Id = item.SiteBlockId, type = item.GetType().BaseType.Name })</td>
                <td class="text-center">
                    @Html.ActionLink("Remove", "Remove", new { Id = item.SiteBlockId, type = item.GetType().BaseType.Name })
                </td>
            </tr>
            }
        </tbody>
    </table>
</body>
</html>
